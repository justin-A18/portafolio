---
interface Props {
	className?: string;
	variant?: 'default' | 'primary' | 'secondary';
}

const { className, variant = 'default' } = Astro.props;

const variants = {
	default: 'text-2xl',
	primary: '',
	secondary: '',
};
---

<h3 class={`${variants[variant]} ${className}`}>
	<slot />
</h3>
