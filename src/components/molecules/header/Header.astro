---
import NavBar from './NavBar.astro';

interface Props {
	className?: string;
}

const { className } = Astro.props;
---

<header
	class={`fixed top-0 z-10 flex items-center justify-center w-full mx-auto mt-2 ${className}`}>
	<NavBar />
</header>
